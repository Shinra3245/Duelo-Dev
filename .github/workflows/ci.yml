# CI de DueloDev — obligatorio desde la Semana 0 (contexto H11).
#
# Etapa actual: evaluación pura del juez. El gate explícito impide añadir
# ejecución de código sin integrar la validación del sandbox (docs/06).
name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '24'

jobs:
  preparacion:
    name: Detectar fases implementadas
    runs-on: ubuntu-latest
    outputs:
      api: ${{ steps.fases.outputs.api }}
    steps:
      - uses: actions/checkout@v4
      - id: fases
        shell: bash
        run: |
          if [[ -f api/package.json ]]; then
            echo "api=true" >> "$GITHUB_OUTPUT"
          else
            echo "api=false" >> "$GITHUB_OUTPUT"
            echo "API e integración pendientes de implementación (F2)." >> "$GITHUB_STEP_SUMMARY"
          fi

  formato:
    name: Formato y lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npm run format:check
      - run: npm run lint

  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npm run typecheck

  unitarios:
    name: Pruebas unitarias
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npm test

  # Se activa en la Semana 3, cuando existan migraciones y endpoints.
  integracion:
    name: Integración (API + Postgres + Redis)
    runs-on: ubuntu-latest
    needs: preparacion
    if: needs.preparacion.outputs.api == 'true'
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: duelodev
          POSTGRES_PASSWORD: test
          POSTGRES_DB: duelodev_test
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U duelodev -d duelodev_test"
          --health-interval 5s --health-timeout 5s --health-retries 20
      redis:
        image: redis:7-alpine
        ports: ['6379:6379']
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s --health-timeout 3s --health-retries 20
    env:
      DATABASE_URL: postgres://duelodev:test@localhost:5432/duelodev_test
      REDIS_URL: redis://localhost:6379
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npm run test:integration --workspace api

  juez:
    name: Juez — evaluación pura y preflight
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: judge/requirements-dev.txt
      - run: pip install -r judge/requirements-dev.txt
      - run: ruff check judge/
      - run: black --check judge/
      - run: mypy judge/
      - run: python -m pytest judge/tests -v
      - name: Estado de la validación del sandbox
        shell: bash
        run: |
          if [[ -f judge/executor.py || -f judge/Dockerfile.judge ]]; then
            echo "::error::Antes de incorporar el ejecutor, configurar aquí la suite S01–S20 en un entorno desechable con rootless validado."
            exit 1
          fi
          echo "Solo evaluación pura verificada. Sandbox S01–S20 pendiente; F1 no está cerrada." >> "$GITHUB_STEP_SUMMARY"

  auditoria:
    name: Auditoría de dependencias
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      # Bloqueante: lo que realmente se despliega.
      - name: Dependencias de produccion
        run: npm audit --omit=dev --audit-level=moderate
      # Informativo: las de desarrollo (vitest/vite) no llegan al servidor y
      # Revisar cada advisory; no asumir que ninguna afecta al entorno de desarrollo.
      - name: Dependencias de desarrollo (informativo)
        continue-on-error: true
        run: npm audit --audit-level=high
